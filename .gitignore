# macOS
.DS_Store

# Environment / secrets
.env
.env.local
*.pem

# Editor
.vscode/
.idea/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Node
node_modules/
dist/
# NB: do not add a bare `build/` here — build-time SOURCE lives in scripts/ and
# was once silently excluded by that pattern, which broke CI while working locally.

# local editor/agent settings
.claude/settings.local.json
